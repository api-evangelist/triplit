{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://www.triplit.dev/schemas/entity",
  "title": "Triplit Entity",
  "description": "A database entity (document) stored in a Triplit collection",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the entity within its collection"
    }
  },
  "required": ["id"],
  "additionalProperties": true
}
